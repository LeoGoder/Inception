services:
  nginx:
    build: ./requirements/nginx
    container_name: nginx
    ports:
      - "443:443"
    restart: always
    networks:
      - inception-net
    volumes:
      - website_files:/var/www/html

  mariadb:
    build: ./requirements/mariadb
    container_name: mariadb
    secrets:
      - db
      - user
    restart: always
    networks:
      - inception-net
    volumes:
      - database:/var/lib/mysql

  wordpress:
    build: ./requirements/wordpress
    container_name: wordpress
    secrets:
      - db
      - user
    env_file:
      - .env
    restart: always
    networks:
      - inception-net
    volumes:
      - website_files:/var/www/html

# bonus part
  adminer:
    build: ./requirements/bonus/adminer
    container_name: adminer
    restart: always
    networks:
      - inception-net

  static:
    build: ./requirements/bonus/static
    container_name: static
    restart: always
    networks:
      - inception-net

  portainer:
    build: ./requirements/bonus/portainer
    container_name: portainer
    restart: always
    networks:
      - inception-net
    secrets:
      - portainer_pwd
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    command: ["/opt/portainer/portainer", "--admin-password-file", "/run/secrets/portainer_pwd"]

secrets:
  db:
    file: ./requirements/secrets/DB.TXT
  user:
    file: ./requirements/secrets/USER.TXT
  portainer_pwd:
    file: ./requirements/secrets/PORTAINER_PWD.TXT

volumes:
  website_files:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lgoderne/data/wordpress

  database:
      driver: local
      driver_opts:
        type: none
        o: bind
        device: /home/lgoderne/data/mariadb

  portainer_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/lgoderne/data/portainer
  
networks:
  inception-net:
    driver: bridge
